#include "main.h"

/**
 * *_memset - check if character is uppercase
 *
 * @c: variable denoting character
 *
 * Return: Always 0.
 */

char *_memset(char *s, char b, unsigned int n)
{
    return (0);
}
