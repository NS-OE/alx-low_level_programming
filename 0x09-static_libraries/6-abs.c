#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * _abs  - print absolute value
 *
 * @n: integer variable
 *
 * Return: Always 0 (Success)
 *
 */
int _abs(int n)
{

int nabs;

nabs = abs(n);

return (nabs);
}
