#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
int alph;

return;

}

int main(void)
{
int plus;

for  (plus = 'a' ; plus <= 'z' ; plus++)
{
putchar(plus);
}
putchar('\n');

return (0);
}
