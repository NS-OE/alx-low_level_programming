#include "main.h"

/**
 * _islower  - if input character is lowercase letter
 *
 * Return: Always 0 (Success)
 *
 */
int _islower(int c)
{

if (c >= 'a' && c <= 'z')
{
return (1);
}

else
{
return (0);
}
}
