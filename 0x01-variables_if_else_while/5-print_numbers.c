#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int plus;

plus = 0;

while (plus < 10)
{
printf("%d", plus);

plus++;
}
printf("\n");

return (0);
}
