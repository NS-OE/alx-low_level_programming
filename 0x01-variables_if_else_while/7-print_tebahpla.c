#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int plus;

for (plus = 'z' ; plus >= 'a' ; plus--)
{
putchar(plus);

}
putchar('\n');

return (0);
}
